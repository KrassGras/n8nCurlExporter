{
  "name": "n8n-curl-exporter",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jest": "^29.0.0"
  }
}
